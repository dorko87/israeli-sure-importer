node_modules/
dist/

# Runtime dirs — created on the host, mounted into container
cache/
browser-data/
logs/

# Secrets — never commit
secrets/*
!secrets/README.md

# User config — contains site-specific URLs, never credentials
config.json
.env

# Internal AI context and session planning — not for public repo
CLAUDE.md
PRD.md
renovate.json
.claude/
docs/superpowers*